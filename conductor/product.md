# Product Context

This fork extends NemoClaw's managed Hermes image for the ClawShell deployment.
It keeps OpenShell credential custody and the managed image integrity boundary
while enabling a user to retrieve an allowed file created in a Hermes sandbox.

This is a fork-specific capability. It does not claim that upstream NemoClaw
supports local stdio MCP servers through its managed MCP workflow.
