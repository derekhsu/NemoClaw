# sandbox-mcp-server

Fork-maintained, stdio-only package for the fixed Hermes
`sandbox-file-uploader` MCP server. It requests signed ClawShell Gateway
download links for files that already exist in the sandbox.

This package intentionally does not log gateway credentials or proxy values.
